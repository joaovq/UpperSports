<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.UpperSports" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.App" parent="@android:style/Theme.DeviceDefault" />

    <style name="Theme.UpperSports.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background to black -->
        <item name="windowSplashScreenBackground">@color/black200</item>
        <!-- Use windowSplashScreenAnimatedIcon to add a drawable or an animated
             drawable. -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <!-- Set the theme of the Activity that follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.UpperSports</item>
    </style>
</resources>